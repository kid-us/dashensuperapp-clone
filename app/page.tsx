import Hero from "@/components/landing/hero";
import AccountLink from "@/components/landing/account-link";

const page = () => {
  return (
    <main>
      <Hero />
      <AccountLink />
    </main>
  );
};

export default page;
